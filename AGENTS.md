# Obsidian Agent Instructions

Before changing this repository, read `ai/README.md` and follow its required reading order.

The `ai/` directory is the persistent project memory for current state, operating rules, architectural decisions, experiment history, failures, and handoff information.

After any meaningful engineering attempt, update the relevant `ai/` records as described in `ai/OPERATING_MANUAL.md`.
